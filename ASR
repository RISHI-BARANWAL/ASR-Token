pragma solidity ^0.6.2;

import '@openzeppelin/contracts/token/ERC20/ERC20.sol';

contract ASR is ERC20 {
    contructor() ERC20('ASR is Stablecoin', 'ASR') public{}
    function faucet(address to, uint amount) external{
        _mint(to, amount);
    }
}
